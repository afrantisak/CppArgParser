language: cpp
compiler:
    - g++
before_install:
    - sudo apt-get install libboost-dev libboost-program-options libboost-system
    - mkdir -p build
    - cd build  
script:  
    - cmake ..
    - make
